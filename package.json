{
  "name": "cpuchain-docker",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "docker compose -f docker-compose.dev.yml build --no-cache",
    "start": "docker compose up -d cpuchain_torrent cpuchain_node1",
    "start:blockbook": "docker compose up -d cpuchain_torrent cpuchain_node1 cpuchain_blockbook",
    "start:mini": "docker compose up -d cpuchain_torrent cpuchain_mini"
  },
  "author": "",
  "license": "ISC"
}
